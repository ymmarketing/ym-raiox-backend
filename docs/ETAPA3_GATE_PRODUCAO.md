# Gate de produção — NÃO EXECUTADO

A integração permanece em branch/staging. Merge em `main` só após homologação integrada e GO explícito da responsável pelo produto. O Asaas atual está em produção; nenhuma cobrança de teste deve ser criada automaticamente.